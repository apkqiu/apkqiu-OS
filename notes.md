### i386 BIOS 启动时内存分布（实模式物理地址）

| 起始地址   | 结束地址   | 大小       | 用途 / 区域              | 说明                                                                 |
|------------|------------|------------|--------------------------|----------------------------------------------------------------------|
| `0x00000`  | `0x003FF`  | 1 KB       | 中断向量表 (IVT)         | 256 个中断向量，每个 4 字节（CS:IP），由 BIOS 和 DOS 使用            |
| `0x00400`  | `0x004FF`  | 256 字节   | BIOS 数据区 (BDA)        | 存储键盘状态、串口参数、软盘信息、内存大小等系统参数                  |
| `0x00500`  | `0x07BFF`  | ~30 KB     | 常规可用内存             | 引导加载程序可自由使用，常被用作临时堆栈或加载内核                    |
| `0x07C00`  | `0x07DFF`  | 512 字节   | 引导扇区 (Boot Sector)   | BIOS 将启动设备的第一扇区加载到此，末尾须有 `0x55AA` 标志            |
| `0x07E00`  | `0x9FBFF`  | ~608 KB    | 常规可用内存             | 引导程序或操作系统可使用的区域，部分可能被 BIOS 或 EBDA 占用         |
| `0x9FC00`  | `0x9FFFF`  | ~1 KB      | EBDA (扩展 BIOS 数据区)  | 通常位于此范围，大小可能因 BIOS 而异（典型 1KB 或更小）              |
| `0xA0000`  | `0xBFFFF`  | 128 KB     | VGA 显存 (帧缓冲)        | 文本/图形模式下的显示内存映射，直接写入即可显示                      |
| `0xC0000`  | `0xC7FFF`  | 32 KB      | 视频 BIOS ROM            | VGA 显卡的固件代码，初始化显示功能                                   |
| `0xC8000`  | `0xEFFFF`  | 128 KB     | BIOS 扩展 ROM            | 其他设备（如 SCSI 卡、网卡、硬盘控制器）的选项 ROM                   |
| `0xF0000`  | `0xFFFFF`  | 64 KB      | 系统 BIOS ROM            | 主板固件，含 POST 自检和引导例程，入口点在 `0xFFFF0`（跳转指令）     |
| `0x100000` | 以上       | 不定       | 扩展内存 (Extended)      | 物理地址 1MB 以上，需开启 A20 并切换保护模式才能访问                 |

---

**补充说明：**
- **常规内存** = `0x00000` ~ `0x9FFFF`（共 640 KB），其中固定占用的只是低端部分，其余为可用区域。
- **上位内存 (UMB)** = `0xA0000` ~ `0xFFFFF`（384 KB），包含显存、ROM 和映射设备。
- EBDA 的具体起始地址可能在 `0x9FC00` 附近，实际需要查询 BIOS 数据区（`0x40E` 处的双字）获得准确位置。
- 扩展内存（>1MB）在启动时通常未初始化，需由引导程序或操作系统管理。

如果需要更细节的某个区间信息，可以再告诉我。